from setuptools import setup, find_packages

setup(
    name='MOC', 
    version='1.0',
    author='Itay Livni',
    author_email='livni.itay@gmail.com',
    packages=find_packages(),
    setup_requires=['setuptools_scm']
    )